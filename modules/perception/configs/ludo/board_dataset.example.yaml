# Ultralytics dataset spec for fine-tuning the Ludo pawn detector.
# `path` is resolved relative to this file's location.

path: ../../data/ludo_board_dataset
train: images/train
val: images/val

names:
  0: red
  1: green
  2: yellow
  3: blue
