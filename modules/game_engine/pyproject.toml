[project]
name = "game-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "baotram153", email = "tramdang7907129@gmail.com" }
]
requires-python = ">=3.12"
dependencies = ["common"]

[tool.uv.sources]
common = { workspace = true }

[dependency-groups]
dev = ["pytest>=8.0"]

[build-system]
requires = ["uv_build>=0.11.30,<0.12.0"]
build-backend = "uv_build"
